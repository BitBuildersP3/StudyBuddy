import { UrlChangeDirective } from './url-change.directive';

describe('UrlChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new UrlChangeDirective();
    expect(directive).toBeTruthy();
  });
});
